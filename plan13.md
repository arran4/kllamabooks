So if we open a document, we do this in `MainWindow::onOpenBooksSelectionChanged`:
Wait, no, the code snippet from `onOpenBooksSelectionChanged` didn't show document viewing. Let's see the rest of `onOpenBooksSelectionChanged`.
