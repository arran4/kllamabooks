"it doesn't seem to be presisting, or syncing between teh tree view and the explorer view"

Wait, I changed `loadDocumentsAndNotes()` to NOT be called in `saveDocBtn` and `moveItemToFolder`?
Let's see what `moveItemToFolder` does!
