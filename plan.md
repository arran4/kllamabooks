1. **Add new checkbox in UI**: In `SettingsDialog.cpp` & `SettingsDialog.h`, add a `QCheckBox* m_prioritizeSameModelCheck;` with text "Prioritize jobs with the same model as just ran". Load/save it to `QSettings("prioritizeSameModel", false)`.
2. **Update queue logic**: In `QueueManager.h`, add a member variable `QString m_lastProcessedModel;`. When a job finishes (`QueueManager::onComplete` or `QueueManager::onError`), store `m_currentItem.model` in `m_lastProcessedModel`.
3. **Update sort logic**: In `QueueManager::processNext()`, if `QSettings().value("prioritizeSameModel").toBool()` is true, modify the sort lambda: if one item's model matches `m_lastProcessedModel` and the other doesn't, prioritize the one that matches. If both match or neither match, fall back to the selected `processingMode`.
