Wait! Is `updateNotificationStatus` clearing the UI badges if we just clear it in the DB?
Yes!

In `MainWindow::onOpenBooksSelectionChanged`, what does it do?
`onOpenBooksSelectionChanged` is what is fired when we click the item (single click).
Does it view the document?
Let's see.
